import React from 'react';
import { Text, View } from 'react-native';

const ListaPeliculas = (props) => {
	return (
		<View>
			<Text>Lis de películas</Text>
		</View>
	);
};

export default ListaPeliculas;
