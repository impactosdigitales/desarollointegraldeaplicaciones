import React from 'react';
import { Text, View } from 'react-native';

const ListaGeneros = (props) => {
	return (
		<View>
			<Text>Lista de géneros</Text>
		</View>
	);
};

export default ListaGeneros;
